// config
var startURL = "";
var wanted = "";

// modules
const request = require("request");
const cheerio = require("cheerio");
const fs = require("fs");

// sub functions
var loadPage = (url) => {};
var getLinks = (page) => {};
var catLinks = (links) => {};

// main function
var crawl = (url) => {};